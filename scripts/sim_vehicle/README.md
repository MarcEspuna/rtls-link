## TODOs

- Modify script to use files for sending a sequence of commands (Maybe read files and output samples through the socket). 
- Move the host and port to the python argument command line.
- Add a command to list all the available commands.
- Ask GPT for ideas on how to improve the script.
